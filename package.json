{
  "name": "providentia-projection",
  "version": "1.0.0",
  "description": "Kinetic projection slides — Providentia",
  "scripts": {
    "start": "serve public -l 3000",
    "build": "echo 'Static site — no build step required'",
    "dev": "serve public -l 3000"
  },
  "dependencies": {
    "serve": "^14.2.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
